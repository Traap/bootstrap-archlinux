#!/bin/bash
# {{{ Notes
#
# Notes:
#   Options are listed alphabetically only because I scan them faster.
#
#shellcheck disable=SC2034
#
# -------------------------------------------------------------------------- }}}
# {{{ Products to install.

base16ColorsFlag=false
bspwmFlag=false
desktopAppsFlag=false
echoConfigFlag=false
gitBashPromptFlag=false
gitsFlag=false
heyMailFlag=false
hyprlandFlag=false
kjvFlag=false
luarocksPackages=false
mirrorFlag=false
msDotnetAppsFlag=false
msWindowsAppsFlag=false
myReposFlag=false
neovimBuildFlag=false
neovimPluginsFlag=false
nodeJsFlag=true
nvimsFlag=false
osUpdateFlag=false
osUpdateKeysFlag=false
pacmanPackagesFlag=false
pipPackagesFlag=false
rbenvFlag=false
removePersonalizationFlag=false
resolvFlag=false
rustFlag=false
sshDirFlag=false
sshHostKeyFlag=false
symlinksFlag=false
texPackagesFlag=false
timezoneFlag=false
tmuxPluginsFlag=false
vimPluginsFlag=false
wslFlag=false
yayPackagesFlag=false

# -------------------------------------------------------------------------- }}}
# {{{ Other variables used during product installation.

# The root personal repositories are cloned to.
cloneRoot=$HOME/git

# The email assiciated with git commit messages.
gitEmail="gary.a.howard@mac.com"

# Git user namn used for commit messages.
gitUserName="Traap"

# Ruby version to install.  rbenv is the Ruby enviornment manager.
# rubyVerion becomes the global Ruby version installed.
rubyVersion='3.3.6'

# Host name the Arch linux installation is known by.  The default host name
# is the Windows 10 or Windows 11 host name.
wslHostName=Zero
wslUserName=Traap

# Reflector location is used to update the mirror list.
reflectorLocation="United States"

# Timezone
timezone=America/Los_Angeles

# -------------------------------------------------------------------------- }}}
